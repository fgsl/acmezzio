name: PHP Composer

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Instala dependências
      run: composer install

    - name: Executa os testes
      run: php -S localhost:8000 -t public && vendor/bin/phpunit
      

